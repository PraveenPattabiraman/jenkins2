pipeline {
    agent any
    stages {
        stage (' httpd install') {
            agent {
                docker {
                    image 'httpd'
                }
            }
            steps {
                sh 'docker run -itd --name httpdcontainer -p 9090:80 httpd'
            }
        }

    }
}