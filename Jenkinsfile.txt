pipeline {
    agent any
    stages {
        stage ('build stage') {
            sh 'docker build -t httpd:v1'
        }
        stage {
            sh 'docker run -itd --name httpdcontainer httpd:v1'
        }
    }
}