pipeline {
    agent {
        docker { image { 'httpd' }}
    }
    stages {
        stage ('build') {
            steps {
                sh 'docker run -itd --name httpdcontainer -p 8090:80 httpd'
            }
        }

    }
}